{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "cross-env SKIP_NATIVE_OXIDE=1 tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "postinstall": "node -e \"try { require('@swc/core') } catch (e) { console.warn('Optional dependencies missing, will use JS implementation') }\"",
    "use-babel-plugin": "bun remove @vitejs/plugin-react-swc && bun add @vitejs/plugin-react && node -e \"const fs=require('fs');const path=require('path');const configPath=path.resolve('vite.config.ts');let config=fs.readFileSync(configPath,'utf8');config=config.replace(/react-swc/g,'react');fs.writeFileSync(configPath,config);\""
  },
  "dependencies": {
    "@tailwindcss/vite": "catalog:tailwindcss",
    "react": "catalog:react",
    "react-dom": "catalog:react",
    "tailwindcss": "catalog:tailwindcss"
  },
  "devDependencies": {
    "@eslint/js": "catalog:eslint",
    "@types/react": "catalog:react",
    "@types/react-dom": "catalog:react",
    "@vitejs/plugin-react-swc": "^4.2.3",
    "cross-env": "^7.0.3",
    "eslint": "catalog:eslint",
    "eslint-plugin-react-hooks": "^5.1.0",
    "eslint-plugin-react-refresh": "^0.5.0",
    "globals": "^17.3.0",
    "typescript": "catalog:typescript",
    "typescript-eslint": "^8.56.1",
    "vite": "^7.3.1"
  }
}
